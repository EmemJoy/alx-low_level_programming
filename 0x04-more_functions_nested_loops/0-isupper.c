#include "main.h"
/**
* _isupper - checks for uppercase character
* @x: Variable text
* Return: Return 1 for uppercase and 0 for any else.
*/
int _isupper(int x)
{

 if (x >= 65 && x <= 90)
 {
 return (1);
 }
 return (0);
}
